({
    onAgentWorkloadChanged : function(cmp, event, helper)
    {
        helper.agentWorkloadChange(cmp,event);
    },
     onAgentWorkAccepted : function(cmp, event, helper)
    {
        helper.agentWorkAccepted(cmp,event);
           }
})